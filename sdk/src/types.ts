export interface Project {
  id: string;
  apiKey: string;
  name: string;
  description: string;
  logoUrl: string;
  referralReward: string;
  incentives: string[];
  primaryColor: string;
  secondaryColor: string;
}